// Dinonaktifkan sementara

<div class="container mt-5">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <h4>Verifikasi OTP</h4>
            <p>Masukkan kode OTP yang telah dikirim ke WhatsApp Anda.</p>

            <?php if ($this->session->flashdata('error')): ?>
                <div class="alert alert-danger"><?php echo $this->session->flashdata('error') ?></div>
            <?php endif; ?>

            <form method="post" action="<?php echo base_url('otp/verify'); ?>">
                <div class="mb-3">
                    <label for="otp_code" class="form-label">Kode OTP</label>
                    <input type="text" id="otp_code" name="otp_code" class="form-control" required maxlength="6" pattern="\d{6}" placeholder="Masukkan 6 digit kode">
                </div>
                <button type="submit" class="btn btn-success">Verifikasi</button>
            </form>

            <form method="post" action="<?php echo base_url('otp/resend'); ?>" class="mt-2">
                <button type="submit" class="btn btn-link p-0">🔄 Kirim Ulang OTP</button>
            </form>
        </div>
    </div>
</div>
